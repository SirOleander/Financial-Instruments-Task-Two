# Dashboard assets

Drop a **`logo.png`** (or update `app.py`) in this folder to replace the placeholder
"LOGO" box at the top-left of the sidebar. The app auto-detects `dashboard/assets/logo.png`
and renders it in place of the placeholder — no code change needed.

Recommended: a transparent-background PNG, roughly 3:1 to square, ~240px wide.
